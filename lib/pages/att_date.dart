import 'package:flutter/material.dart';

class ViewAttendenceByDate extends StatelessWidget {
  // const ViewAttendenceByClass({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('View By Class is Coming Soon!!!')),
    );
  }
}
